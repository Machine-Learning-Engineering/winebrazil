tags:
  - Comercializacao
consumes:
  - application/json
parameters:
  - name: Authorization
    in: header
    type: string
    required: true
    description: Token de autenticação JWT  
responses:
  200:
    description: Atualização processada com sucesso
    schema:
      type: object
      properties:
        status:
          type: string