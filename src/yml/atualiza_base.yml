tags:
  - Comercializacao
consumes:
  - application/json
parameters:
  - in: body
    name: body
    required: true
responses:
  200:
    description: Atualização processada com sucesso
    schema:
      type: object
      properties:
        status:
          type: string